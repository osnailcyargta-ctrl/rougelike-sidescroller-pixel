# The snapshot is generated by snapshot.sh from the repository itself. Keeping
# a copy in git would mean two versions of the game in one branch, and the
# stale one would eventually ship.
app/src/main/assets/www/
build/
app/build/
.gradle/
local.properties
